<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wangxinenpu.springbootdemo.dao.mapper.SysMatterExecuteDetailMapper" >
	<resultMap id="SysMatterExecuteDetail" type="com.wangxinenpu.springbootdemo.dataobject.po.SysMatterExecuteDetail" >
		<!--
		  @haoxz11MyBatis
		  事项执行配置表
		-->
		<id column="id" property="id" jdbcType="BIGINT" />
		<result column="config_id" property="configId" jdbcType="BIGINT" />
		<result column="sid" property="sid" jdbcType="VARCHAR" />
		<result column="matter_id" property="matterId" jdbcType="BIGINT" />
		<result column="matter_name" property="matterName" jdbcType="VARCHAR" />
		<result column="manage_code" property="manageCode" jdbcType="VARCHAR" />
		<result column="create_time" property="createTime" jdbcType="TIMESTAMP" />
		<result column="modify_time" property="modifyTime" jdbcType="TIMESTAMP" />
	</resultMap>
	<insert id="insertSysMatterExecuteDetail" parameterType="com.wangxinenpu.springbootdemo.dataobject.po.SysMatterExecuteDetail" useGeneratedKeys="true" >
		<!--
		  @haoxz11MyBatis
		  插入事项执行配置表记录
		-->
		<selectKey resultType="java.lang.Long" keyProperty="id" order="AFTER" >
			SELECT LAST_INSERT_ID() AS id
		</selectKey>
		<![CDATA[
		insert into sys_matter_execute_detail (config_id, sid, matter_id, matter_name,manage_code,create_time, modify_time)
		values (#{configId,jdbcType=BIGINT}, #{sid,jdbcType=VARCHAR}, #{matterId,jdbcType=BIGINT},#{matterName,jdbcType=VARCHAR},
		#{manageCode,jdbcType=VARCHAR},sysdate(), sysdate())
		]]>
	</insert>
	<update id="updateSysMatterExecuteDetail" parameterType="com.wangxinenpu.springbootdemo.dataobject.po.SysMatterExecuteDetail" >
		<!--
		  @haoxz11MyBatis
		  更新事项执行配置表记录
		-->
		update sys_matter_execute_detail
		<set >
			<if test="configId != null" >
				config_id = #{configId,jdbcType=BIGINT},
			</if>
			<if test="sid != null" >
				sid = #{sid,jdbcType=VARCHAR},
			</if>
			<if test="matterId != null" >
				matter_id = #{matterId,jdbcType=BIGINT},
			</if>
			<if test="matterName != null" >
				matter_name = #{matterName,jdbcType=VARCHAR},
			</if>
			<if test="manageCode != null" >
				manage_code = #{manageCode,jdbcType=VARCHAR},
			</if>
			modify_time = sysdate()
		</set>
		where id = #{id,jdbcType=BIGINT}
	</update>
	<select id="getByPrimaryKey" resultMap="SysMatterExecuteDetail" parameterType="java.lang.Long" >
		<!--
		  @haoxz11MyBatis
		  根据主键查询事项执行配置表记录
		-->
		select  * from sys_matter_execute_detail where id = #{id,jdbcType=BIGINT}
	</select>
	<sql id="whereSQL" >
		<!--
		
		  搜索事项执行配置表记录，可修改
		-->
		<where >
			<if test="id != null">
				<![CDATA[ AND id = #{id,jdbcType=BIGINT} ]]>
			</if>
			<if test="configId != null" >
				<![CDATA[ AND config_id = #{configId,jdbcType=BIGINT} ]]> 
			</if>
			<if test="sid != null" >
				<![CDATA[ AND sid = #{sid,jdbcType=VARCHAR} ]]> 
			</if>
			<if test="matterId != null" >
				<![CDATA[ AND matter_id = #{matterId,jdbcType=BIGINT} ]]> 
			</if>
			<if test="matterName != null" >
				<![CDATA[ AND matter_name = #{matterName,jdbcType=VARCHAR} ]]> 
			</if>
			<if test="manageCode != null" >
				<![CDATA[ AND manage_code = #{manageCode,jdbcType=VARCHAR} ]]> 
			</if>
		</where>
	</sql>
	<select id="getListByWhere" resultMap="SysMatterExecuteDetail" parameterType="map" >
		<!--
		  @haoxz11MyBatis
		  搜索事项执行配置表列表
		-->
		select
		<if test="FIELDS != null" >
			${FIELDS}
		</if>
		<if test="FIELDS == null" >
			*
		</if>
		from sys_matter_execute_detail
		<include refid="whereSQL" />
		<if test="ORDERLIST != null" >
			 ORDER BY ${ORDERLIST}
		</if>
	</select>
	<select id="getCountByWhere" resultType="java.lang.Integer" parameterType="map" >
		<!--
		  @haoxz11MyBatis
		  得到搜索事项执行配置表的记录数量
		-->
		select count(*) from sys_matter_execute_detail
		<include refid="whereSQL" />
	</select>
	
	<!--根据ID删除 -->
	<delete id="deleteById"  parameterType="java.lang.Long">
		delete from sys_matter_execute_detail where id = #{id,jdbcType=BIGINT}
	</delete>
	
	<delete id="deleteByConfigId"  parameterType="java.lang.Long">
		delete from sys_matter_execute_detail where config_id = #{configId,jdbcType=BIGINT}
	</delete>
</mapper>